import operate from './operate';

const calculate = () => {
  const numbers = ['9', '8', '7', '6', '5', '4', '3', '2', '1', '0'];

  const operators = ['/', '*', '+', '-'];
};

export default calculate;
